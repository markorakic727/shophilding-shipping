{% if customer %}

	{% capture captured_page %}
    <h1 class="page-title">{{ page.title }}</h1>

    {% if page.content != blank %}
      <div class="rte">
        {{ page.content }}
      </div>
    {% endif %}

    {% section 'module-faq' %}

    {% endcapture %}
    {% include 'password-protect' %}

{% else %}

	{% include 'please-login' %}

{% endif %}







